import styled, { css } from "styled-components";
import { motion } from "framer-motion";

const ChangeThemeButton = styled(motion.div)`
  display: flex;
  background: transparent;
  cursor: pointer;
  font-size: 25px;
`;

export { ChangeThemeButton };
